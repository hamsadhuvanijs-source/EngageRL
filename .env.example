# Backend (paths are relative to the backend/ directory, since that's where uvicorn/alembic are run from)
GEMINI_API_KEY=
DATABASE_URL=sqlite:///./app.db
UPLOAD_DIR=./uploads
CORS_ORIGINS=http://localhost:3000

# Frontend
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
